# TODO

- iTerm2 (friend's setup): picker rendered differently than expected. Suspected
  `fzf` interop — decide whether wtclaude should detect/defer to `fzf` or always
  use the native picker. (The robot mascot is now the separate
  [`egor-xyz/robot`](https://github.com/egor-xyz/robot) plugin, so animation
  porting is no longer part of this question.)
